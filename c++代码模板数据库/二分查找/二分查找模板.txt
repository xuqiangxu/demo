//二分最主要条件是确认所要查找的值是否存在区域中
//存在则根据条件即可判断left,right状态
//否则需要进一步判断
while(l<r)
{
    int m=l+(r-l)/2;//溢出预防
    if(条件)  r=mid;
    else  l=mid+1;
}
最终l=r停留在某个边界

/********另一种二分模板
while(l<r)
{
    int m=l+(r-l+1)/2;//溢出预防
    if(条件)  r=mid-1;
    else  l=mid;
}
